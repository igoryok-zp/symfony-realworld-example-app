grumphp:
  tasks:
    phpcs:
      standard: PSR12
      ignore_patterns:
        - "config/reference.php"
    phpmd:
      exclude:
        - "config/reference.php"
    phpstan:
      level: max
      ignore_patterns:
        - "config/reference.php"
